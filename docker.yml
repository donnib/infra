---

- hosts: "{{host}}"
  become: true
  vars_files:
    - 'vars/vault.yaml'
  vars_prompt:
    - name: host
      prompt: Which host?
      private: no
    - name: containers
      prompt: Which containers?
      private: no
  tasks:
    - name: Docker Pull
      command: "docker-compose pull {{containers}}"
      args:
        chdir: /home/{{main_username}}/

    - name: Docker Update
      command: "docker-compose up -d {{containers}}"
      args:
        chdir: /home/{{main_username}}/

    - name: Docker prune
      command: "docker image prune -f"